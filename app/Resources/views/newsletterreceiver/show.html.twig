{% extends 'base.html.twig' %}

{% block body %}
    <h1>Newsletterreceiver</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ newsletterReceiver.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ newsletterReceiver.email }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('newsletterreceiver_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('newsletterreceiver_edit', { 'id': newsletterReceiver.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
